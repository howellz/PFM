@model IEnumerable<PFM.Models.Subcategories>
@{
    ViewData["Title"] = "Transactions";
}

<h1>Transactions</h1>

<p>
    <a asp-area="" asp-controller="Transactions" asp-action="Create" asp-route-userID="@ViewBag.userID"> Create New</a>
</p>

<div id="TabsSet1">
    <ul>
        @foreach (var i in ViewBag.Categories)
        {
            if (i.UserId == ViewBag.userID)
            {
                <li><a href="#@i.CategoryName">@i.CategoryName</a></li>
            }
        }
    </ul>
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <form asp-action="Edit">
        @foreach (var i in ViewBag.Categories)
        {
            if (i.UserId == ViewBag.userID)
            {
                <div id=@i.CategoryName>
                    <table class="table">
                        <thead>
                            <tr>
                                <th>
                                    Subcategory
                                </th>

                                <th>
                                    Transaction Amount
                                </th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in i.Subcategories)
                            {
                                @foreach (var j in item.Transactions)
                                {
                                    <tr>
                                        <td>
                                            @item.SubcategoryName
                                        </td>
                                        <td>
                                            $@j.Value
                                        </td>
                                        <td>
                                            <a asp-area="" asp-controller="Transactions" asp-action="Edit" asp-route-id="@j.TransactionsId">Edit</a>
                                            <a asp-area="" asp-controller="Transactions" asp-action="Details" asp-route-id="@j.TransactionsId">Details</a>
                                            <a asp-area="" asp-controller="Transactions" asp-action="Delete" asp-route-id="@j.TransactionsId">Delete</a>
                                        </td>

                                    </tr>
                                }
                            }
                        </tbody>

                    </table>
                </div>
            }
        }

    </form>

</div>

<script>
    $(function () {
        $("#TabsSet1").tabs();
    });
</script>

