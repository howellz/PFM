@model PFM.Models.Subcategories

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>Subcategory</h4>
<hr />
<h2 class="form-group" style="color:#FF0000">@ViewBag.emssg</h2>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <input id="CategoryId" name="CategoryId" type="hidden" value="@ViewBag.id">

            <div class="form-group">
                Subcategory Name
                <input asp-for="SubcategoryName" class="form-control" />
                <span asp-validation-for="SubcategoryName" class="text-danger"></span>
            </div>
            <div class="form-group">
                Budget
                <input asp-for="Value" class="form-control" />
                <span asp-validation-for="Value" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-controller="Categories"  asp-action="Index" asp-route-userID="@ViewBag.userID ">Back to planner</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
